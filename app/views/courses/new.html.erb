<% content_for :title, "New course" %>

<div class="container mx-auto p-4 flex flex-col items-center space-y-4">
  <h1 class="text-center align-text-top border-lime-200">Create a New Course</h1>
  <div class="w-full">
    <%= render "form", course: @course %>
  </div>
  <div>
    <%= link_to "Back to courses", courses_path %>
  </div>
</div>
